<li>
	<small class="text-muted">
		<%= post.created_at.localtime.strftime("%B %e, %Y") %>
	</small>
	<p>
		<% if post.url %>
			<a href="<%= post.url %>">
				<%= post.short_message %>
			</a>
		<% else %>
			<%= post.short_message %>
		<% end %>
	</p>
</li>
